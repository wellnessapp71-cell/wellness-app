# Web app auth settings
# Use a long random value in local/dev. Do not commit real secrets.
JWT_SECRET=replace-with-a-random-64-char-secret
JWT_EXPIRY=7d

# Compatibility mode for legacy clients without tokens.
# true  -> allow legacy userId fallback when token is not provided
# false -> require valid Bearer token for protected endpoints
AUTH_COMPAT=true
